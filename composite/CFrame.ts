export class CFrame {
    public x: number;
    public y: number;
    public width: number;
    public height: number;

    // TODO: Адаптировать фрейм под объект, который он покрывает

    constructor(x: number, y: number, width: number, height: number) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}